Program Example7;
var n : double;
    s : string;
    x : integer;
    y : integer;
Function f(n: double; s : string ) : string ;
begin
     if n < 0 then f:= " " else f := f(n-1, s) + f(n-1, s);
end;

Begin
     call writeS(F(10.5, "hello"));
end.